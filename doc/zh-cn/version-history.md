# 版本历史

MyDay!!!!! 的逐版本摘要。在改动一个行为之前理解它*为什么*存在很有用——多条记录记录的是刻意的安全修复，否则看起来像怪癖。

## 版本

- `v0.1.0`：squash 后的首个公开发布；待办、财务、亲密、体重、WebDAV 同步、备份、ZIP/CSV 导入/导出、四语言本地化、GPLv3、隐私政策、CI/CD、macOS 支持。
- `v0.1.4`：伴侣/玩具日期、自定义待办 emoji 输入、相关 l10n。
- `v0.1.5`：玩具购买链接/价格、GPL 许可证页、设置打磨。
- `v0.2.0`-`v0.2.3`：广泛的 l10n 清理、托盘/提醒语言区域感知、图表日期标签修复、默认独自亲密和姿势管理。
- `v0.3.0`：体重图表改进、亲密双轴趋势图、安装包版本修复、BOM 清理。
- `v0.3.1`：体重提醒、亲密页滚动布局、短范围图表标签修复。
- `v0.3.2`：L10n 审计修复、转账分类、财务 AppBar 重构、左对齐模块标题。
- `v0.4.0`：本地 HTTP API 服务器、开机自启、桌面设置扩展、公共配置 API、macOS 网络服务器授权。
- `v0.5.0`：仅引用图像同步、同步警告、亲密时长趋势图、交易金额计算器键盘。
- `v0.5.1`：ARM64 安装包文件名修复、趋势图日期重叠修复、亲密趋势线原始 + EWMA。
- `v0.6.0`：一次性任务的待办重复，以及用 7 天滚动窗口的频率图尖峰修复。
- `v0.6.1`：模型/UI/API 中的可选待办备注。
- `v0.6.2`：待办、财务账户和亲密的激活/历史自定义排序。
- `v0.6.3`：待办子任务保存安全/重排和主要对话框的未保存编辑保护。
- `v0.6.4`：周历史分组、体重提醒宽限窗口、零值图表过滤、未知 JSON 字段保留。
- `v0.6.5`：财务月度总计、更平滑的趋势采样、总资产趋势图、强制余额历史重建，版本 `0.6.5+24`。
- `v0.6.6`：财务强制余额被迁移为普通调整交易，实时余额现在只来自交易，旧版兼容使用 `0 + 1970-01-01` 强制余额哨兵，版本统一为 `0.6.6+25` / MSIX `0.6.6.0` / 安装包 `0.6.6`。
- `v0.6.7`：财务账户可以存储每月最低余额和/或每月转入的可选免手续费标准，版本统一为 `0.6.7+26` / MSIX `0.6.7.0` / 安装包 `0.6.7`。
- `v0.7.0`：财务分析自定义日期范围可以在不丢失先前选择的情况下重新编辑，到期时取消的订阅被排除在即将续费提醒之外，财务主页交易带月份选择按月过滤，账户交易页可以在账户预选的情况下添加交易，亲密默认历史显示带显示全部面板的有限近期列表，版本统一为 `0.7.0+27` / MSIX `0.7.0.0` / 安装包 `0.7.0`。
- `v0.7.1`：财务分析中支出、收入和未分类流的分类行打开带增/删/改支持的下钻交易页，分类添加流程自动预选分类/类型，版本统一为 `0.7.1+28` / MSIX `0.7.1.0` / 安装包 `0.7.1`。
- `v0.7.2`：订阅计费生成对每个订阅计费日幂等，新生成的订阅交易使用稳定 ID，历史订阅导入跳过既有计费日，交易账户选择器设置支持名称/自定义排序、类型分组和账户页的更多账户，版本统一为 `0.7.2+29` / MSIX `0.7.2.0` / 安装包 `0.7.2`。
- `v0.7.3`：亲密伴侣和玩具记录详情页支持添加、编辑和删除相关记录，显示平均愉悦度和时长摘要，含过滤的愉悦度/时长趋势图，版本统一为 `0.7.3+30` / MSIX `0.7.3.0` / 安装包 `0.7.3`。
- `v0.7.4`：亲密记录支持带 x100/x1 单位的可选抽插次数，主页和伴侣/玩具趋势页的时长图添加单独抽插次数轴，亲密 CSV 导入/导出包含新字段，版本统一为 `0.7.4+31` / MSIX `0.7.4.0` / 安装包 `0.7.4`。
- `v0.7.5`：亲密秒表会话在意外页面/应用退出后持续，计时器历史条目可以确认为运行中会话并恢复，亲密记录跟踪安全套使用，CSV 导入/导出包含安全套字段，版本统一为 `0.7.5+32` / MSIX `0.7.5.0` / 安装包 `0.7.5`。
- `v0.7.6`：亲密计时器添加带 +100/-100 控件的非负 x100 抽插计数器，计时器会话/历史为记录预填和恢复保留该计数，版本统一为 `0.7.6+33` / MSIX `0.7.6.0` / 安装包 `0.7.6`。
- `v0.7.7`：亲密趋势图为组合数据系列使用更高对比度颜色，安全套保护记录块显示肯定状态文本，版本统一为 `0.7.7+34` / MSIX `0.7.7.0` / 安装包 `0.7.7`。
- `v0.7.8`：体重记录支持 cm 单位的可选胸/腰/臀测量，体重趋势图添加左 kg 右 cm 的双轴测量线，体重 CSV/API 流程包含新字段，版本统一为 `0.7.8+35` / MSIX `0.7.8.0` / 安装包 `0.7.8`。
- `v0.7.9`：体重摘要卡片显示最新胸/腰/臀值加腰臀比和紧凑颜色条，体重和身体测量趋势拆分为带更宽松轴的单独原始/EWMA 图表，版本统一为 `0.7.9+36` / MSIX `0.7.9.0` / 安装包 `0.7.9`。
- `v0.7.10`：一次性待办提醒从排定日期开始并每日重复直到完成，带排定非每日待办条目的未来日历日期显示专用标记，版本统一为 `0.7.10+37` / MSIX `0.7.10.0` / 安装包 `0.7.10`。
- `v0.7.11`：待办主页为所选日期显示内联周历同时保留月选择器，待办日支持按天存储和同步的 -5 到 5 日评分，版本统一为 `0.7.11+38` / MSIX `0.7.11.0` / 安装包 `0.7.11`。
- `v0.7.12`：待办日评分措辞现在描述全天评分，月历作为带内联年/月跳转的次级页打开，日评分趋势图含零值，快乐日和痛苦日列表，版本统一为 `0.7.12+39` / MSIX `0.7.12.0` / 安装包 `0.7.12`。
- `v0.7.13`：全局设置可以选择哪一天开始一周，应用日历/日期选择器使用共享本地化星期/月标签，待办/亲密日历和体重/亲密周分组跟随所选起始日，版本统一为 `0.7.13+40` / MSIX `0.7.13.0` / 安装包 `0.7.13`。
- `v0.7.14`：移动端一次性待办提醒不再早于排定日期开始；未来一次性任务在激活前使用一次性开始日期通知，提醒重新调度时清除过期的任务通知 ID，版本统一为 `0.7.14+41` / MSIX `0.7.14.0` / 安装包 `0.7.14`。
- `v0.7.15`：体重摘要卡片和身体测量趋势图按字段从最近的先前正值继承缺失的胸/腰/臀值，同时让每条记录的存储空字段保持不变；iOS 启动器图标添加安全区默认、深色和着色变体；Windows CI 设置临时 VS/MSVC 18 coroutine 弃用兼容标志；版本统一为 `0.7.15+42` / MSIX `0.7.15.0` / 安装包 `0.7.15`。
- `v0.8.0`：为待办、财务和体重刷新本地 HTTP API，带更严格的 Basic Auth、富化任务/交易/体重负载、日评分和财务账户/分类端点、转换后的财务摘要、转账感知交易创建、身体成分体重写入和本地 API 回归测试；版本统一为 `0.8.0+43` / MSIX `0.8.0.0` / 安装包 `0.8.0`。
- `v1.0.0`：发布前审计加固——WebDAV 下载区分 404 与错误，使瞬态失败永远不可能覆盖远程或级联成跨设备删除；上传失败（含 ETag `If-Match` 412 冲突）浮出为逐文件同步错误而不是静默成功；跨模块冲突解决不再崩溃，未解决冲突默认本地记录；所有 `modifiedAt`/设置时间戳以 UTC 写入；月末订阅计费日期钳制而不是跳过月份；汇率合并绝不保留悬空的当前快照 id；体重身高跟随设置 LWW；进程内提醒循环仅桌面，带 ≥-时间到期语义和持久化的按天去重；移动订阅提醒是按日一次性通知，内容按天准确；体重宽限跳过保持每日重复；已删除/已完成的每日模板停止提醒；经 `flutter_timezone` 解析 IANA 时区；15 分钟周期自动同步加配置保存时同步；基于允许列表的 ZIP 导入；财务摘要上的缺失汇率警告；移除未使用的 `SCHEDULE_EXACT_ALARM`；版本统一为 `1.0.0+44` / MSIX `1.0.0.0` / 安装包 `1.0.0`。
- `v1.0.1`：添加亲密玩具管理激活成本摘要、全部/激活/退役玩具的玩具成本总览（带激活/全部每日成本趋势和最终退役成本）、单玩具总/每日成本摘要、玩具管理副标题中每个玩具的每日成本，更新 GitHub Actions stable Flutter 到 `3.44.2`，本地配置 GitHub 远程，版本统一为 `1.0.1+45` / MSIX `1.0.1.0` / 安装包 `1.0.1`。
- `v1.0.2`：订阅可以在原地恢复待定的到期时取消，并通过把已过期/立即取消的订阅复制进新的可编辑激活订阅来恢复；亲密计时器添加 +50/+10 抽插控件，非 100 倍数计数自动 x1 存储，外加用 `wakelock_plus` 记住的仅本地保持屏幕唤醒开关；版本统一为 `1.0.2+46` / MSIX `1.0.2.0` / 安装包 `1.0.2`。
- `v1.1.0`：加固财务和数据导入安全，使损坏的财务 JSON 不能被当作空数据处理，保存串行化且原子化，ZIP/JSON 导入和备份恢复在替换前校验已知数据，WebDAV 自动同步失败/冲突可见且不再在后台用 LWW 解决，版本统一为 `1.1.0+47` / MSIX `1.1.0.0` / 安装包 `1.1.0`。
- `v1.1.1`：设置导入/导出重建为仅 ZIP 并移除 CSV/JSON 文件导入流程；WebDAV 上传改用带稳定本地客户端 id 和 150 秒 TTL 的远程 `.lock`，中断的本地上传在下一次同步被检测，HTTP 412 上传竞争重新下载远程数据并重新运行逐记录合并，然后才浮出真正的记录冲突；版本统一为 `1.1.1+48` / MSIX `1.1.1.0` / 安装包 `1.1.1`。
- `v1.1.2`：WebDAV 在下载和合并远程数据之前获取 `.lock`，把锁 TTL 降低到 60 秒，并在有效锁下强制上传完整的合并/已解决 JSON，不再有数据文件 `If-Match`/`If-None-Match` 重试循环；版本统一为 `1.1.2+49` / MSIX `1.1.2.0` / 安装包 `1.1.2`。
- `v1.1.3`：ZIP 数据导入现在严格把 JSON 条目解码为 UTF-8，使中文和其他非 ASCII 文本被保留；版本统一为 `1.1.3+50` / MSIX `1.1.3.0` / 安装包 `1.1.3`。
- `v1.2.0`：WebDAV 同步加固和强制传输——远程图像列目录失败不再伪装成空目录（修复已上传图像的重复重新上传），瞬态网络错误和 HTTP 5xx 带退避重试，同步进度通过 `WebDAVService.progress` 发布并显示为带本地化阶段文本的进度条，WebDAV 页面新增带确认对话框的强制上传/强制下载操作，自动同步获得可重入守卫并在开始前忽略 `notifySaved`，体重提醒宽限窗口锚定在实际触发时间上，使在排定分钟之后记录的记录能抑制迟到的桌面提醒（带单元测试），下载的图像触发 UI 重载，手动同步通知重载监听器，同步对话框中的图像警告本地化，修复待办字符串中的 zh_TW 简体字符泄漏，WebDAV 术语和 `…` 省略号在 MyAnime/MyDay/MyDevice 间标准化，关于页版本格式与其他应用一致（`version+build`），版本统一为 `1.2.0+51` / MSIX `1.2.0.0` / 安装包 `1.2.0`。
- `v1.2.1`：前台同步操作（手动同步、冲突最终化、强制上传/下载）通过新 `sync_wake_lock.dart` 复用 `wakelock_plus` 持有屏幕唤醒锁，在完成/失败/取消/异常时的 `finally` 中释放，并与亲密计时器的锁安全共存；备份保留新增 3 天选项；八个重复的 `finance*` ARB 键在全部四个语言区域中去重（保留当前生效值），移除未使用的 `settingsImport` 键；设置/备份术语与 MyAnime/MyDevice 标准化（"Export Data"、"Create Backup"、"Retention Period"、"Keep forever"、"History ({count})"、"Backup created"、可操作的 API 凭据警告、"Auto-sync"、"Storage location updated/reset to default"）；最后一个 ASCII `...` 转换为 `…`；版本统一为 `1.2.1+52` / MSIX `1.2.1.0` / 安装包 `1.2.1`。
- `v1.2.2`：备份大改——备份格式 v2 把图像只存一次在内容寻址的 `backups/blobs/` 存储中，所有备份共享，带引用计数 GC（blob 只在没有剩余备份引用它时删除；旧内联图像捆绑仍可恢复），捆绑写入原子化且损坏捆绑在历史中标记（禁用恢复），且不抑制每日自动备份重试；恢复备份现在重载打开的页面和移动提醒日程，在同步已配置时禁用 WebDAV 自动同步，并提供对恢复数据的显式强制上传（防止恢复出的旧数据把删除传播到其他设备；旧的"请重启应用"提示消失）；此前未本地化的 `weight` 恢复模块在全部四个语言区域获得 `backupModuleWeight` 标签和图标；`BackupService` 配置 I/O 现在走 `TodoStorage.readConfig()/writeConfig()`；设置备份条目获得共享的"Full local backup (data + images)"副标题；备份 UI 文本/CJK 术语与 MyAnime/MyDevice 标准化（"Auto Backup"、"Select All"、规范的恢复/删除确认）；版本统一为 `1.2.2+53` / MSIX `1.2.2.0` / 安装包 `1.2.2`。
- `v1.2.3`：同步/备份安全加固和 Android 构建现代化——恢复备份现在在第一个文件被写入*之前*禁用 WebDAV 自动同步，并且只在恢复失败且未写入任何内容时重新启用（带 `ok`/`wroteAnything`/`missingImages` 的 `RestoreResult`）；缺失的 v2 备份图像 blob 浮出本地化的 `backupRestoreMissingImages` 警告，而不是被静默丢弃；WebDAV 数据/图像 PUT 每 20 秒心跳刷新远程 `.lock`，使比 60 秒 TTL 更慢的传输不再可能把第二个客户端竞态成一次丢失更新；GitHub Actions 升级到 checkout v7 / setup-java v5 / upload-artifact v7 / download-artifact v8 / gh-release v3（移除 Node 20 弃用警告）；Android 迁移到 Gradle 9.3.1 + AGP 9.1.1，应用侧 Built-in Kotlin 迁移在 `builtInKotlin=false` 兼容标志下完成；`file_picker` 精确固定为 `10.3.7`；版本统一为 `1.2.3+54` / MSIX `1.2.3.0` / 安装包 `1.2.3`。
- `v1.2.4`：亲密身体层——一个性别中立、全部可选、自动保存的身体设置子页加入伴侣/玩具/姿势管理菜单（用户胸/腰/臀从最新体重记录镜像，带可抑制警告，其确认编辑每批恰好创建一条新体重记录，绝不修改历史；仅本地退出键 `intimacyBodyWeightSyncWarningDisabled`），下胸围加 EU/EN 13402、FR/ES、JP/JIS（罩杯优先显示）、UK、US 和 AU/NZ 标准的罩杯估算，带帮助对话框和估算免责声明，每个身体界面上的只读腰臀比，阴茎测量带 PSI 参考指数（基于 dm 的截锥公式、中性措辞、Wang Cuntong 等人 2020 仅作为统计参考引用），以及月经周期跟踪（默认关），带记录的开始日期、锚定在最近实际开始日上的基于中位数预测、阶段/生育窗口/排卵估计和非避孕/非医疗免责声明；伴侣详情页变成记录 | 身体标签，带完全独立的逐伴侣身体数据，绝不同步到体重模块；主页日历为每个启用显示选项的人叠加周期，带稳定逐人颜色、逐人指示器行、图例和所选日归属条；同步获得 `cycleRecords`（按 id 合并使删除传播）和 `userBody` 按 `userBodyModifiedAt` LWW，带匹配的 JSON 保留模式和新单元测试（`body_metrics_test`、`cycle_predictor_test`、`intimacy_merge_test`）；所有添加都是默认关的向后兼容可选字段，版本统一为 `1.2.4+55` / MSIX `1.2.4.0` / 安装包 `1.2.4`。
- `v1.2.5`：通过串行化和原子替换亲密、体重、待办和汇率数据文件，修复伴侣删除损坏或隐藏所有亲密记录的问题；亲密/体重/待办现在对不可读的既有文件抛出类型化异常，其页面显示阻塞的本地化恢复状态并拒绝保存，只读提醒调用方跳过损坏数据，身体体重编辑安全中止，本地 API 返回 HTTP 500 `data_unreadable`。删除伴侣也会移除该伴侣的周期记录，编辑伴侣已被删除的历史亲密记录不再触发下拉断言。添加存储并发/损坏回归测试和已删除伴侣对话框组件测试；版本统一为 `1.2.5+56` / MSIX `1.2.5.0` / 安装包 `1.2.5`。
- `v1.3.0`：共享 `myapps_data` 包抽取——WebDAV 同步、备份/恢复、ZIP 传输、原子写入和自动同步调度现在来自嵌入在 `packages/myapps_data` 的 git 子模块共享包，MyAnime、MyDay 和 MyDevice 都构建在它之上；`webdav_service.dart` 从 2291 行变成 244 行，服务变成薄门面，因此全部 132 个既有测试无需修改全部通过，且完全没有 golden 变更。MyDay 特有行为以显式钩子而非抹除的方式保留：财务强制余额迁移作为 `postMergeTransform` 恰好在其先前的位置运行（合并*之后*和冲突解决之后），汇率保持永远不冲突的整文件并集合并，未知字段保留保持模式驱动且在应用侧，因为 MyDay 合并输出不是自我保留的，每日备份保持由 `ReminderService` 的 30 秒循环驱动，而不是共享调度器。五个硬编码数据文件清单副本中的四个消失，被 `lib/app/data_modules.dart` 取代。版本统一为 `1.3.0+57` / MSIX `1.3.0.0` / 安装包 `1.3.0`。
- `v1.3.1`：修复自定义存储路径迁移。更改存储文件夹以前移动五个数据文件和 `webdav_config.json`，把 `images/`、`.sync_base/` 和 `backups/`（捆绑和 blob）遗留在旧位置。最坏的后果是静默的：没有 `.sync_base/`，下一次同步就没有基线快照，于是其他设备已删除的记录被读成"本地新增"并重新上传，在每台设备上复活删除。迁移现在移动**整个**存储文件夹，唯独不动 `storage_config.json`（它必须留在原地——它保存路径本身），因此未来版本新增的数据文件无需代码变更即可迁移。既有的目标文件仍然优先，它们的源副本现在留在原地而不是被丢弃。共享逻辑位于 `myapps_data` `v1.0.1` 的 `migrateStorageContents`，由八个测试覆盖。版本统一为 `1.3.1+58` / MSIX `1.3.1.0` / 安装包 `1.3.1`。
- `v1.3.2`：亲密抽插速率和一个整合趋势图。`IntimacyRecord` 获得两个派生的、绝不持久化的 getter——`resolvedThrustCount`（存储计数乘其 x1/x100 单位）和 `thrustsPerMinute`（条目的平均抽插速率，只在条目同时有时长和抽插次数时计算）。模块的四个记录指标图坍缩为一个 `IntimacyTrendChart` 组件，主页和伴侣/玩具详情页都用：愉悦度、频率、时长、抽插次数和新抽插速率都可选，默认开愉悦度 + 时长 + 抽插速率。因为这些指标量纲不兼容，绘图区是无单位的 0–1 空间，每个系列对照自己的吸附天花板在其中归一化；前两个所选指标拥有以真实单位标注的左右轴，其余从工具提示读取，指标过滤芯片兼作图例，因此不需要单独图例行。图表拒绝清除最后一个所选指标，因此它绝不渲染为空。指标选择和时间范围作为 `intimacy_data.json` 中新的可选 `chartSettings` 对象持久化和同步，在既有的 `settingsModifiedAt` 最后写入者胜出时钟上合并，并被每个表面共享——主页拥有写入，详情页通过记录和排序回调已经使用的同一回调链上报，因此文件仍只有一个写入者。指标和范围标识符存储为字符串而不是枚举索引并逐字往返，因此未来构建的 id 经当前构建走一趟也能存活；对象在用户首次更改选择前被完全省略，这正是 WebDAV golden 转录逐字节相同的原因。删除重复图表代码从 `intimacy_page.dart`（7178 → 5642）移除 27 个声明和约 1,550 行，把五个近乎相同的 EWMA 点构建器坍缩为一个、四个内联吸附辅助坍缩为一个逐指标描述符、三个范围到截断的 switch 副本坍缩为一个；玩具每日成本图保留但共享公共 `IntimacyChartRange` 枚举。伴侣和玩具详情页还在平均愉悦度和时长旁显示平均抽插速率。18 个新测试（共 150 个，全部通过）。版本统一为 `1.3.2+59` / MSIX `1.3.2.0` / 安装包 `1.3.2`。
