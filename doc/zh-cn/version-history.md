# 版本历史

MyDay!!!!! 的逐版本摘要。在改动一个行为之前理解它*为什么*存在很有用——多条记录记录的是刻意的安全修复，否则看起来像怪癖。

## 版本

- `v0.1.0`：squash 后的首个公开发布；待办、财务、亲密、体重、WebDAV 同步、备份、ZIP/CSV 导入/导出、四语言本地化、GPLv3、隐私政策、CI/CD、macOS 支持。
- `v0.1.4`：伴侣/玩具日期、自定义待办 emoji 输入、相关 l10n。
- `v0.1.5`：玩具购买链接/价格、GPL 许可证页、设置打磨。
- `v0.2.0`-`v0.2.3`：广泛的 l10n 清理、托盘/提醒语言区域感知、图表日期标签修复、默认独自亲密和姿势管理。
- `v0.3.0`：体重图表改进、亲密双轴趋势图、安装包版本修复、BOM 清理。
- `v0.3.1`：体重提醒、亲密页滚动布局、短范围图表标签修复。
- `v0.3.2`：L10n 审计修复、转账分类、财务 AppBar 重构、左对齐模块标题。
- `v0.4.0`：本地 HTTP API 服务器、开机自启、桌面设置扩展、公共配置 API、macOS 网络服务器授权。
- `v0.5.0`：仅引用图像同步、同步警告、亲密时长趋势图、交易金额计算器键盘。
- `v0.5.1`：ARM64 安装包文件名修复、趋势图日期重叠修复、亲密趋势线原始 + EWMA。
- `v0.6.0`：一次性任务的待办重复，以及用 7 天滚动窗口的频率图尖峰修复。
- `v0.6.1`：模型/UI/API 中的可选待办备注。
- `v0.6.2`：待办、财务账户和亲密的激活/历史自定义排序。
- `v0.6.3`：待办子任务保存安全/重排和主要对话框的未保存编辑保护。
- `v0.6.4`：周历史分组、体重提醒宽限窗口、零值图表过滤、未知 JSON 字段保留。
- `v0.6.5`：财务月度总计、更平滑的趋势采样、总资产趋势图、强制余额历史重建，版本 `0.6.5+24`。
- `v0.6.6`：财务强制余额被迁移为普通调整交易，实时余额现在只来自交易，旧版兼容使用 `0 + 1970-01-01` 强制余额哨兵，版本统一为 `0.6.6+25` / MSIX `0.6.6.0` / 安装包 `0.6.6`。
- `v0.6.7`：财务账户可以存储每月最低余额和/或每月转入的可选免手续费标准，版本统一为 `0.6.7+26` / MSIX `0.6.7.0` / 安装包 `0.6.7`。
- `v0.7.0`：财务分析自定义日期范围可以在不丢失先前选择的情况下重新编辑，到期时取消的订阅被排除在即将续费提醒之外，财务主页交易带月份选择按月过滤，账户交易页可以在账户预选的情况下添加交易，亲密默认历史显示带显示全部面板的有限近期列表，版本统一为 `0.7.0+27` / MSIX `0.7.0.0` / 安装包 `0.7.0`。
- `v0.7.1`：财务分析中支出、收入和未分类流的分类行打开带增/删/改支持的下钻交易页，分类添加流程自动预选分类/类型，版本统一为 `0.7.1+28` / MSIX `0.7.1.0` / 安装包 `0.7.1`。
- `v0.7.2`：订阅计费生成对每个订阅计费日幂等，新生成的订阅交易使用稳定 ID，历史订阅导入跳过既有计费日，交易账户选择器设置支持名称/自定义排序、类型分组和账户页的更多账户，版本统一为 `0.7.2+29` / MSIX `0.7.2.0` / 安装包 `0.7.2`。
- `v0.7.3`：亲密伴侣和玩具记录详情页支持添加、编辑和删除相关记录，显示平均愉悦度和时长摘要，含过滤的愉悦度/时长趋势图，版本统一为 `0.7.3+30` / MSIX `0.7.3.0` / 安装包 `0.7.3`。
- `v0.7.4`：亲密记录支持带 x100/x1 单位的可选抽插次数，主页和伴侣/玩具趋势页的时长图添加单独抽插次数轴，亲密 CSV 导入/导出包含新字段，版本统一为 `0.7.4+31` / MSIX `0.7.4.0` / 安装包 `0.7.4`。
- `v0.7.5`：亲密秒表会话在意外页面/应用退出后持续，计时器历史条目可以确认为运行中会话并恢复，亲密记录跟踪安全套使用，CSV 导入/导出包含安全套字段，版本统一为 `0.7.5+32` / MSIX `0.7.5.0` / 安装包 `0.7.5`。
- `v0.7.6`：亲密计时器添加带 +100/-100 控件的非负 x100 抽插计数器，计时器会话/历史为记录预填和恢复保留该计数，版本统一为 `0.7.6+33` / MSIX `0.7.6.0` / 安装包 `0.7.6`。
- `v0.7.7`：亲密趋势图为组合数据系列使用更高对比度颜色，安全套保护记录块显示肯定状态文本，版本统一为 `0.7.7+34` / MSIX `0.7.7.0` / 安装包 `0.7.7`。
- `v0.7.8`：体重记录支持 cm 单位的可选胸/腰/臀测量，体重趋势图添加左 kg 右 cm 的双轴测量线，体重 CSV/API 流程包含新字段，版本统一为 `0.7.8+35` / MSIX `0.7.8.0` / 安装包 `0.7.8`。
- `v0.7.9`：体重摘要卡片显示最新胸/腰/臀值加腰臀比和紧凑颜色条，体重和身体测量趋势拆分为带更宽松轴的单独原始/EWMA 图表，版本统一为 `0.7.9+36` / MSIX `0.7.9.0` / 安装包 `0.7.9`。
- `v0.7.10`：一次性待办提醒从排定日期开始并每日重复直到完成，带排定非每日待办条目的未来日历日期显示专用标记，版本统一为 `0.7.10+37` / MSIX `0.7.10.0` / 安装包 `0.7.10`。
- `v0.7.11`：待办主页为所选日期显示内联周历同时保留月选择器，待办日支持按天存储和同步的 -5 到 5 日评分，版本统一为 `0.7.11+38` / MSIX `0.7.11.0` / 安装包 `0.7.11`。
- `v0.7.12`：待办日评分措辞现在描述全天评分，月历作为带内联年/月跳转的次级页打开，日评分趋势图含零值，快乐日和痛苦日列表，版本统一为 `0.7.12+39` / MSIX `0.7.12.0` / 安装包 `0.7.12`。
- `v0.7.13`：全局设置可以选择哪一天开始一周，应用日历/日期选择器使用共享本地化星期/月标签，待办/亲密日历和体重/亲密周分组跟随所选起始日，版本统一为 `0.7.13+40` / MSIX `0.7.13.0` / 安装包 `0.7.13`。
- `v0.7.14`：移动端一次性待办提醒不再早于排定日期开始；未来一次性任务在激活前使用一次性开始日期通知，提醒重新调度时清除过期的任务通知 ID，版本统一为 `0.7.14+41` / MSIX `0.7.14.0` / 安装包 `0.7.14`。
- `v0.7.15`：体重摘要卡片和身体测量趋势图按字段从最近的先前正值继承缺失的胸/腰/臀值，同时让每条记录的存储空字段保持不变；iOS 启动器图标添加安全区默认、深色和着色变体；Windows CI 设置临时 VS/MSVC 18 coroutine 弃用兼容标志；版本统一为 `0.7.15+42` / MSIX `0.7.15.0` / 安装包 `0.7.15`。
- `v0.8.0`：为待办、财务和体重刷新本地 HTTP API，带更严格的 Basic Auth、富化任务/交易/体重负载、日评分和财务账户/分类端点、转换后的财务摘要、转账感知交易创建、身体成分体重写入和本地 API 回归测试；版本统一为 `0.8.0+43` / MSIX `0.8.0.0` / 安装包 `0.8.0`。
- `v1.0.0`：发布前审计加固——WebDAV 下载区分 404 与错误，使瞬态失败永远不可能覆盖远程或级联成跨设备删除；上传失败（含 ETag `If-Match` 412 冲突）浮出为逐文件同步错误而不是静默成功；跨模块冲突解决不再崩溃，未解决冲突默认本地记录；所有 `modifiedAt`/设置时间戳以 UTC 写入；月末订阅计费日期钳制而不是跳过月份；汇率合并绝不保留悬空的当前快照 id；体重身高跟随设置 LWW；进程内提醒循环仅桌面，带 ≥-时间到期语义和持久化的按天去重；移动订阅提醒是按日一次性通知，内容按天准确；体重宽限跳过保持每日重复；已删除/已完成的每日模板停止提醒；经 `flutter_timezone` 解析 IANA 时区；15 分钟周期自动同步加配置保存时同步；基于允许列表的 ZIP 导入；财务摘要上的缺失汇率警告；移除未使用的 `SCHEDULE_EXACT_ALARM`；版本统一为 `1.0.0+44` / MSIX `1.0.0.0` / 安装包 `1.0.0`。
- `v1.0.1`：添加亲密玩具管理激活成本摘要、全部/激活/退役玩具的玩具成本总览（带激活/全部每日成本趋势和最终退役成本）、单玩具总/每日成本摘要、玩具管理副标题中每个玩具的每日成本，更新 GitHub Actions stable Flutter 到 `3.44.2`，本地配置 GitHub 远程，版本统一为 `1.0.1+45` / MSIX `1.0.1.0` / 安装包 `1.0.1`。
- `v1.0.2`：订阅可以在原地恢复待定的到期时取消，并通过把已过期/立即取消的订阅复制进新的可编辑激活订阅来恢复；亲密计时器添加 +50/+10 抽插控件，非 100 倍数计数自动 x1 存储，外加用 `wakelock_plus` 记住的仅本地保持屏幕唤醒开关；版本统一为 `1.0.2+46` / MSIX `1.0.2.0` / 安装包 `1.0.2`。
- `v1.1.0`：加固财务和数据导入安全，使损坏的财务 JSON 不能被当作空数据处理，保存串行化且原子化，ZIP/JSON 导入和备份恢复在替换前校验已知数据，WebDAV 自动同步失败/冲突可见且不再在后台用 LWW 解决，版本统一为 `1.1.0+47` / MSIX `1.1.0.0` / 安装包 `1.1.0`。
- `v1.1.1`：设置导入/导出重建为仅 ZIP 并移除 CSV/JSON 文件导入流程；WebDAV 上传改用带稳定本地客户端 id 和 150 秒 TTL 的远程 `.lock`，中断的本地上传在下一次同步被检测，HTTP 412 上传竞争重新下载远程数据并重新运行逐记录合并，然后才浮出真正的记录冲突；版本统一为 `1.1.1+48` / MSIX `1.1.1.0` / 安装包 `1.1.1`。
- `v1.1.2`：WebDAV 在下载和合并远程数据之前获取 `.lock`，把锁 TTL 降低到 60 秒，并在有效锁下强制上传完整的合并/已解决 JSON，不再有数据文件 `If-Match`/`If-None-Match` 重试循环；版本统一为 `1.1.2+49` / MSIX `1.1.2.0` / 安装包 `1.1.2`。
- `v1.1.3`：ZIP 数据导入现在严格把 JSON 条目解码为 UTF-8，使中文和其他非 ASCII 文本被保留；版本统一为 `1.1.3+50` / MSIX `1.1.3.0` / 安装包 `1.1.3`。
- `v1.2.0`：WebDAV 同步加固和强制传输——远程图像列目录失败不再伪装成空目录（修复已上传图像的重复重新上传），瞬态网络错误和 HTTP 5xx 带退避重试，同步进度通过 `WebDAVService.progress` 发布并显示为带本地化阶段文本的进度条，WebDAV 页面新增带确认对话框的强制上传/强制下载操作，自动同步获得可重入守卫并在开始前忽略 `notifySaved`，体重提醒宽限窗口锚定在实际触发时间上，使在排定分钟之后记录的记录能抑制迟到的桌面提醒（带单元测试），下载的图像触发 UI 重载，手动同步通知重载监听器，同步对话框中的图像警告本地化，修复待办字符串中的 zh_TW 简体字符泄漏，WebDAV 术语和 `…` 省略号在 MyAnime/MyDay/MyDevice 间标准化，关于页版本格式与其他应用一致（`version+build`），版本统一为 `1.2.0+51` / MSIX `1.2.0.0` / 安装包 `1.2.0`。
- `v1.2.1`：前台同步操作（手动同步、冲突最终化、强制上传/下载）通过新 `sync_wake_lock.dart` 复用 `wakelock_plus` 持有屏幕唤醒锁，在完成/失败/取消/异常时的 `finally` 中释放，并与亲密计时器的锁安全共存；备份保留新增 3 天选项；八个重复的 `finance*` ARB 键在全部四个语言区域中去重（保留当前生效值），移除未使用的 `settingsImport` 键；设置/备份术语与 MyAnime/MyDevice 标准化（"Export Data"、"Create Backup"、"Retention Period"、"Keep forever"、"History ({count})"、"Backup created"、可操作的 API 凭据警告、"Auto-sync"、"Storage location updated/reset to default"）；最后一个 ASCII `...` 转换为 `…`；版本统一为 `1.2.1+52` / MSIX `1.2.1.0` / 安装包 `1.2.1`。
- `v1.2.2`：备份大改——备份格式 v2 把图像只存一次在内容寻址的 `backups/blobs/` 存储中，所有备份共享，带引用计数 GC（blob 只在没有剩余备份引用它时删除；旧内联图像捆绑仍可恢复），捆绑写入原子化且损坏捆绑在历史中标记（禁用恢复），且不抑制每日自动备份重试；恢复备份现在重载打开的页面和移动提醒日程，在同步已配置时禁用 WebDAV 自动同步，并提供对恢复数据的显式强制上传（防止恢复出的旧数据把删除传播到其他设备；旧的"请重启应用"提示消失）；此前未本地化的 `weight` 恢复模块在全部四个语言区域获得 `backupModuleWeight` 标签和图标；`BackupService` 配置 I/O 现在走 `TodoStorage.readConfig()/writeConfig()`；设置备份条目获得共享的"Full local backup (data + images)"副标题；备份 UI 文本/CJK 术语与 MyAnime/MyDevice 标准化（"Auto Backup"、"Select All"、规范的恢复/删除确认）；版本统一为 `1.2.2+53` / MSIX `1.2.2.0` / 安装包 `1.2.2`。
- `v1.2.3`：同步/备份安全加固和 Android 构建现代化——恢复备份现在在第一个文件被写入*之前*禁用 WebDAV 自动同步，并且只在恢复失败且未写入任何内容时重新启用（带 `ok`/`wroteAnything`/`missingImages` 的 `RestoreResult`）；缺失的 v2 备份图像 blob 浮出本地化的 `backupRestoreMissingImages` 警告，而不是被静默丢弃；WebDAV 数据/图像 PUT 每 20 秒心跳刷新远程 `.lock`，使比 60 秒 TTL 更慢的传输不再可能把第二个客户端竞态成一次丢失更新；GitHub Actions 升级到 checkout v7 / setup-java v5 / upload-artifact v7 / download-artifact v8 / gh-release v3（移除 Node 20 弃用警告）；Android 迁移到 Gradle 9.3.1 + AGP 9.1.1，应用侧 Built-in Kotlin 迁移在 `builtInKotlin=false` 兼容标志下完成；`file_picker` 精确固定为 `10.3.7`；版本统一为 `1.2.3+54` / MSIX `1.2.3.0` / 安装包 `1.2.3`。
- `v1.2.4`：亲密身体层——一个性别中立、全部可选、自动保存的身体设置子页加入伴侣/玩具/姿势管理菜单（用户胸/腰/臀从最新体重记录镜像，带可抑制警告，其确认编辑每批恰好创建一条新体重记录，绝不修改历史；仅本地退出键 `intimacyBodyWeightSyncWarningDisabled`），下胸围加 EU/EN 13402、FR/ES、JP/JIS（罩杯优先显示）、UK、US 和 AU/NZ 标准的罩杯估算，带帮助对话框和估算免责声明，每个身体界面上的只读腰臀比，阴茎测量带 PSI 参考指数（基于 dm 的截锥公式、中性措辞、Wang Cuntong 等人 2020 仅作为统计参考引用），以及月经周期跟踪（默认关），带记录的开始日期、锚定在最近实际开始日上的基于中位数预测、阶段/生育窗口/排卵估计和非避孕/非医疗免责声明；伴侣详情页变成记录 | 身体标签，带完全独立的逐伴侣身体数据，绝不同步到体重模块；主页日历为每个启用显示选项的人叠加周期，带稳定逐人颜色、逐人指示器行、图例和所选日归属条；同步获得 `cycleRecords`（按 id 合并使删除传播）和 `userBody` 按 `userBodyModifiedAt` LWW，带匹配的 JSON 保留模式和新单元测试（`body_metrics_test`、`cycle_predictor_test`、`intimacy_merge_test`）；所有添加都是默认关的向后兼容可选字段，版本统一为 `1.2.4+55` / MSIX `1.2.4.0` / 安装包 `1.2.4`。
- `v1.2.5`：通过串行化和原子替换亲密、体重、待办和汇率数据文件，修复伴侣删除损坏或隐藏所有亲密记录的问题；亲密/体重/待办现在对不可读的既有文件抛出类型化异常，其页面显示阻塞的本地化恢复状态并拒绝保存，只读提醒调用方跳过损坏数据，身体体重编辑安全中止，本地 API 返回 HTTP 500 `data_unreadable`。删除伴侣也会移除该伴侣的周期记录，编辑伴侣已被删除的历史亲密记录不再触发下拉断言。添加存储并发/损坏回归测试和已删除伴侣对话框组件测试；版本统一为 `1.2.5+56` / MSIX `1.2.5.0` / 安装包 `1.2.5`。
- `v1.3.0`：共享 `myapps_data` 包抽取——WebDAV 同步、备份/恢复、ZIP 传输、原子写入和自动同步调度现在来自嵌入在 `packages/myapps_data` 的 git 子模块共享包，MyAnime、MyDay 和 MyDevice 都构建在它之上；`webdav_service.dart` 从 2291 行变成 244 行，服务变成薄门面，因此全部 132 个既有测试无需修改全部通过，且完全没有 golden 变更。MyDay 特有行为以显式钩子而非抹除的方式保留：财务强制余额迁移作为 `postMergeTransform` 恰好在其先前的位置运行（合并*之后*和冲突解决之后），汇率保持永远不冲突的整文件并集合并，未知字段保留保持模式驱动且在应用侧，因为 MyDay 合并输出不是自我保留的，每日备份保持由 `ReminderService` 的 30 秒循环驱动，而不是共享调度器。五个硬编码数据文件清单副本中的四个消失，被 `lib/app/data_modules.dart` 取代。版本统一为 `1.3.0+57` / MSIX `1.3.0.0` / 安装包 `1.3.0`。
- `v1.3.1`：修复自定义存储路径迁移。更改存储文件夹以前移动五个数据文件和 `webdav_config.json`，把 `images/`、`.sync_base/` 和 `backups/`（捆绑和 blob）遗留在旧位置。最坏的后果是静默的：没有 `.sync_base/`，下一次同步就没有基线快照，于是其他设备已删除的记录被读成"本地新增"并重新上传，在每台设备上复活删除。迁移现在移动**整个**存储文件夹，唯独不动 `storage_config.json`（它必须留在原地——它保存路径本身），因此未来版本新增的数据文件无需代码变更即可迁移。既有的目标文件仍然优先，它们的源副本现在留在原地而不是被丢弃。共享逻辑位于 `myapps_data` `v1.0.1` 的 `migrateStorageContents`，由八个测试覆盖。版本统一为 `1.3.1+58` / MSIX `1.3.1.0` / 安装包 `1.3.1`。
- `v1.3.2`：亲密抽插速率和一个整合趋势图。`IntimacyRecord` 获得两个派生的、绝不持久化的 getter——`resolvedThrustCount`（存储计数乘其 x1/x100 单位）和 `thrustsPerMinute`（条目的平均抽插速率，只在条目同时有时长和抽插次数时计算）。模块的四个记录指标图坍缩为一个 `IntimacyTrendChart` 组件，主页和伴侣/玩具详情页都用：愉悦度、频率、时长、抽插次数和新抽插速率都可选，默认开愉悦度 + 时长 + 抽插速率。因为这些指标量纲不兼容，绘图区是无单位的 0–1 空间，每个系列对照自己的吸附天花板在其中归一化；前两个所选指标拥有以真实单位标注的左右轴，其余从工具提示读取，指标过滤芯片兼作图例，因此不需要单独图例行。图表拒绝清除最后一个所选指标，因此它绝不渲染为空。指标选择和时间范围作为 `intimacy_data.json` 中新的可选 `chartSettings` 对象持久化和同步，在既有的 `settingsModifiedAt` 最后写入者胜出时钟上合并，并被每个表面共享——主页拥有写入，详情页通过记录和排序回调已经使用的同一回调链上报，因此文件仍只有一个写入者。指标和范围标识符存储为字符串而不是枚举索引并逐字往返，因此未来构建的 id 经当前构建走一趟也能存活；对象在用户首次更改选择前被完全省略，这正是 WebDAV golden 转录逐字节相同的原因。删除重复图表代码从 `intimacy_page.dart`（7178 → 5642）移除 27 个声明和约 1,550 行，把五个近乎相同的 EWMA 点构建器坍缩为一个、四个内联吸附辅助坍缩为一个逐指标描述符、三个范围到截断的 switch 副本坍缩为一个；玩具每日成本图保留但共享公共 `IntimacyChartRange` 枚举。伴侣和玩具详情页还在平均愉悦度和时长旁显示平均抽插速率。18 个新测试（共 150 个，全部通过）。版本统一为 `1.3.2+59` / MSIX `1.3.2.0` / 安装包 `1.3.2`。
- `v1.3.3`：体重提醒宽限窗口修复——三个缺陷合起来使该功能看起来既不存在又失灵。"已有记录则跳过"控件自 `v0.6.4` 起就存在，却**够不着**：`_showReminderSettings` 打开的是默认模态底部面板，Flutter 把它限制在屏幕高度的 9/16，而其主体是不可滚动的 `Column`，因此宽限窗口磁贴——最后一个子项——在 `twice` 模式下以及在矮窗口或大文字缩放下被裁切到看不见。面板现在是 `isScrollControlled` 加 `SingleChildScrollView` 主体，与应用中其他六个面板一致（包括同一文件中它上方 25 行的那个）。第二，桌面检查把窗口锚定在 `current`——30 秒循环恰好评估它的时刻——这是*替换*而不是扩展排定时间窗口：3 小时宽限、08:00 提醒时，06:00 的称重在检查晚于三小时后运行时就不再抑制提醒，因此 13:00 打开应用会为在到期前两小时*之前*已记录的体重触发提醒。`shouldSkipWeightReminderAt` 获得可选的 `scheduledAt` 锚，窗口现在是连续区间 `[scheduledAt − grace, firesAt + 1 min)`；除非 `scheduledAt` 早于 `firesAt` 否则被忽略，因此窗口绝不会向前扩展越过触发时刻，而它最初为之引入的 `v1.2.0` 迟到检查情形得以保留并由测试守卫。移动端无需改动——它已经锚定在排定候选上，那正是字面语义。第三，在体重页之外写入的体重记录——来自亲密身体层和 `POST /weight/add`——从不通知 `ReminderService`，因此在移动端（通知由 OS 提前排定而非触发时决定）挂起的提醒会对着陈旧宽限窗口触发；两条路径现在都调用 `refreshMobileSchedules()`。`_loadData` 另外把早间/晚间提醒时间按 `reminderMode` 门控，与 `ReminderService._refreshWeightDataFromStorage` 早已做的完全一致，因此仍为已禁用模式携带时间的文件无法把它们推进调度器。11 个新测试（共 161 个，全部通过）：`test/weight_reminder_grace_test.dart` 中第二组的 8 个宽限窗口用例，外加 `test/weight_reminder_sheet_test.dart`——本仓库第一个页面级组件测试，对着修复前的面板它精确复现了这个缺陷，报出 `twice` 模式下 134 像素的 `RenderFlex` 溢出（`once` 下 62 像素），以及宽限窗口磁贴在 640 像素高的表面上被布局在 y=738，比底边低 98 像素。因为 `testWidgets` 体在伪异步时间中运行，该测试用交替的 `tester.runAsync` 轮转和 pump 来驱动页面真实的 `dart:io` 加载链，而不是在此永不安定的 `pumpAndSettle`。无数据格式变更，因此 WebDAV golden 转录逐字节相同，函数索引计数器不变。版本统一为 `1.3.3+60` / MSIX `1.3.3.0` / 安装包 `1.3.3`。
- `v1.4.0`：自适应布局基础和外壳导航栏——采用 MyAnime 中总结、并写入 `ADAPTIVE-LAYOUT-GUIDE.md` 的布局约定的三个分阶段版本中的第一个。新的策略模块 `lib/shared/utils/adaptive_layout.dart` 把所有阈值和所有钳制都放在具名谓词之后，并且**不引入任何东西**——连 `package:flutter/*` 都不引入——因此全部六条规则都是纯函数，在毫秒级内于五十个视口上验证，而不是数秒内验证五个。三个独立的问题得到三条独立的规则，没有任何一条经由另一条转发：`canSplitLayout(width, height)` 问的是整个屏幕的*形状*（600 dp 宽、480 dp 高、0.82 的宽高比，三者缺一不可）；`useNavigationRail(screenWidth)` 只问宽度；`columnCapacity(contentWidth, minItemWidth:)` 问给定最小宽度的列能放下几个，分子里加了一个间隙，使算式支付列*之间*的间隙。宽高比测试是承重的那一条，它的存在源于一台设备：Galaxy Z Fold 8 展开后是 4:3 横向面板，因此竖持时它是 3:4——相对高度比它所取代的近方形 Fold 7 更窄——于是同一台设备在同一个宽度上需要两个不同的答案，这是任何宽度阈值都做不到的。代价事先接受：平板竖持时也保持单栏，因为这条规则关乎形状而不是设备类别。`ShellScaffold` 现在把同一份目的地列表渲染成既有的底部 `NavigationBar`，或从 600 dp 宽度起渲染成居中的 `NavigationRail`（`groupAlignment: 0`，因为默认的顶部对齐是给位于前导菜单按钮或 FAB 之下的导航栏用的而这个两者都没有），并被包装成在导航栏仍可能出现的紧凑高度上滚动而不是溢出。两种界面都来自新的 `_destinations` 辅助方法，因此亲密目的地只在唯一一处按可见性标志过滤，两种渲染不可能漂移。仅宽度的规则是刻意的：导航栏帮助最大的恰恰是分栏规则拒绝之处——手机横持在 915 x 412，底部导航栏花掉 19% 的高度，而 915 dp 的宽度闲置。`lib/shared/widgets/adaptive_tile_grid.dart` 添加了 `adaptiveTileRow`/`adaptiveTileRows`（用 `Expanded` 单元格的 `Row` 而不是 `GridView`，因为 MyDay 的列表是外层滚动视图的子组件，而财务列表依赖 `ListView.builder` 虚拟化；单列分支原样返回图块，因此既有的 `Dismissible` 包装得以存活）和 `listColumnsButton`，在只放得下一列时是隐藏而不是禁用。四个 ARB 全部新增四个 l10n 键。新的 `doc/*/adaptive-layout.md` 记录了两处刻意偏离指南之处：MyDay 不需要 `shellListBottomInset`，因为它的 `Scaffold` 没有设置 `extendBody`，而 `SizedBox(height: 80)` 是在两种模式下都正确的 FAB 让位；以及存储的列数偏好只保留给外壳自己的列表界面。Android 清单本就声明了 `screenLayout|screenSize|smallestScreenSize|density`，正是它让折叠能在不重建 activity 的情况下缩放窗口——现在连同理由记录在 `platform-notes.md` 中。尚未改动任何页面布局，也没有数据格式变更，因此 WebDAV golden 转录逐字节相同。30 个新测试（共 191 个，全部通过），包括本仓库第一个纯布局规则测试套件（其每个视口都点名它所代表的真实设备），以及一个用简体中文驱动的外壳导航组件测试，因为 `flutter_test` 的默认字体把拉丁字形渲染成完整全角方块并把它们撑大约 2.5 倍。版本统一为 `1.4.0+61` / MSIX `1.4.0.0` / 安装包 `1.4.0`。
- `v1.4.1`：外壳内的五个页面采用自适应布局规则——三个阶段中的第二个。**待办**在窗口既有形状又有宽度时把三个任务分区并排放置。单位刻意是*分区*而不是图块：每个 `TaskSectionWidget` 包着一个 shrink-wrap 的 `ReorderableListView`，把任务在同一分区的不同列之间拖动没有意义，因此并排的是分区本身，并被轮流发牌进各自独立滚动的列，这样很长的每日清单就不会把工作清单挤出底部。**财务**分成一个容纳月度摘要和即将续订条的固定左窗格，以及一个容纳交易列表的右窗格；堆叠时，那份摘要在第一笔交易出现之前就要花掉手机高度的三分之一。它的图块也在*每个日期分组之内*打包成列，因此日期标题始终横跨整个宽度，一个日期的交易绝不会挨着另一个日期的。**体重**把摘要卡片放到趋势图旁边，其闸门有三个条件而不是指南通常的两个：形状规则、宽度下限（280 + 380 + 12 = 672），以及*至少两条记录*——因为低于两条时图表什么都不渲染，而一张摘要卡片孤零零地待在 280 的窗格里、旁边是一片空白，比它所要取代的堆叠布局更糟。只要一个块可能渲染成空，它就属于闸门的一部分。**亲密**把月历及其周期条保留在自己的窗格里，旁边是趋势图和历史；堆叠时日历本身就占掉手机的大部分高度，因此选中一个日期会把它所选出的记录滚出视野。**设置**成为真正的双栏界面：分区列表留在左侧，`›` 行所通向的页面填满右侧，就像系统设置应用那样。那四个页面——WebDAV、备份、许可证、隐私——都被原样托管，因为只持有一条路由的嵌套 `Navigator` 报告 `canPop == false`，因此它们的 app bar 不会长出返回箭头；每一行都经由同一个 `_open` 方法，因此推入模式和托管模式不会漂移，而 `_twoPane` 在 `build` 中缓存，因此一次点击的行为始终与它发生时屏幕上的样子一致。Flutter 自带的 `showLicensePage` 在两种模式下都保持推入，因为那是本应用并不拥有的页面。四个设备本地的列数偏好（`todoSectionColumns`、`financeListColumns`、`weightListColumns`、`intimacyListColumns`）加入从不同步的 `storage_config.json`，因为窗口尺寸是设备的属性而不是账户的属性；每一个在渲染时都是被钳制而不是拒绝，因此在桌面上固定的列数能挺过折叠并在展开时回来，而设置它的控件在只放得下一列的地方是隐藏而不是禁用。工作中顺带得到两个自身就值得的健壮性修复：体重摘要卡片的大数字和变化块现在是带省略号的 `Flexible`，因此卡片不会溢出新布局交给它的 280 dp 窗格；设置页两个仅桌面的加载器（`launchAtStartup.isEnabled`、`PackageInfo.fromPlatform`）现在会软失败，而此前一个不受支持的平台会因为一个开关磁贴而拖垮整个设置页。任何同步文件的数据格式都没有变化，因此 WebDAV golden 转录逐字节相同。五个文件中新增 42 个测试（共 233 个，全部通过）——每个页面一个文件，这是刻意的，因为 `flutter test` 隔离的是文件而不是文件内的测试，而 `TodoStorage` 的静态配置缓存加上 `ReminderService` / `AutoSyncService` 单例，会在页面共用一个 isolate 时让失败在不同运行之间游走。`intimacy_page.dart` 的玩具/伴侣摘要卡片中还剩一处内联断点，安排在第三阶段处理。版本统一为 `1.4.1+62` / MSIX `1.4.1.0` / 安装包 `1.4.1`。
- `v1.4.2`：所有用 `Navigator.push` 到达的页面采用自适应布局规则——三个阶段中的第三个，也是采用工作的收尾。**最后一处内联断点已消失**：`intimacy_page.dart` 的玩具/伴侣摘要卡片自本次工作之前很久就为其指标网格保留着 `constraints.maxWidth >= 720 / >= 360`，现在和其他一切一样，以明确的 160 dp 最小值走 `columnCapacity`。`grep -rnE "maxWidth *[<>]=? *[0-9]|size\.width *[<>]=? *[0-9]" lib/` 终于在整棵树上为空，而这个断言直到现在才做出，是因为在兄弟应用里同样的断言曾提前一个版本做出，其依据是一次只覆盖了那个版本自己改过文件的搜索。第四条规则加入前三条：**页面拿不能分栏的宽度做什么。** 不是每个页面都有两样东西可以并排，而把一列内容横跨桌面窗口拉伸只会更糟——1400 逻辑像素宽的 `ListTile` 把标题和尾部控件放在屏幕两端。这些页面改为通过新的 `AdaptiveContentWidth` 设上限并居中：表单 720（备份、WebDAV、身体、伴侣与玩具管理列表、订阅），散文 840（许可证、隐私政策、玩具成本总览），所有表单对话框 640——对话框是经由 `Dialog.insetPadding` 而不是新包装器达成的，因此九个对话框各只需一行，各自的组件树无需改动。规则 D 刻意只看宽度且没有闸门：比自己上限更窄的页面在任何视口上都不受影响，因此手机渲染的东西不可能改变。又落地两处双重闸门：分析饼图在 612 dp 以上把图例放到旁边，待办月历在 712 以上把评分趋势图放到旁边——Z Fold 5 竖持通过形状规则却通不过第二重闸门，而那正是该闸门存在的情形。计时器页把会话历史放进秒表旁边 320 dp 的窗格，因为堆叠时很长的历史会把秒表推出屏幕顶端——并在历史为空时回退到堆叠，因为只要一个块可能渲染成空，它就属于闸门的一部分。账户、分类、姿势和汇率获得图块列；三个交易详情页获得交易列；它们全都仅用自动模式，不带存储偏好也不带列数按钮，因为设置界面不该为每个子页面长出一个控件。Emoji 和图标选择器不再使用固定列数：八列时 320 dp 的对话框给每个单元格约 36 dp，低于 Material 的 44 dp 最小触摸目标，因此它们现在从那个目标推导列数，并在宽对话框上增加列。这些页面每一个测量的都是**自己**的宽度而不是 `shellContentWidth`——它们绘制在外壳之上并盖住导航栏，扣除导航栏会悄悄丢掉 81 dp。无数据格式变更，因此 WebDAV golden 转录逐字节相同。25 个新测试（共 258 个，全部通过），其中包括断言图表在每个闸门之上始终满足其下限、对话框内容在任何屏幕宽度下都不超过其上限、以及每个选择器单元格在任何宽度下都满足触摸目标的不变量循环。版本统一为 `1.4.2+63` / MSIX `1.4.2.0` / 安装包 `1.4.2`。
- `v1.4.3`：填满 v1.4.1 的分栏在展开的折叠屏上留下半空的窗格。**财务**——仅在双栏排布中，摘要窗格现在在即将续费条下方承载一个订阅概览：订阅页的三个数字（月应付、月均、年均）作为卡片，经新的 `subscriptionStatMinWidth` 按窗格自己的宽度两张或三张一行打包，以及进行中的订阅作为只读行，点击打开与订阅页相同的详情页，另有一个 chevron 通往订阅页本身。堆叠时该区块刻意不出现——在手机上它会把第一笔交易推得更靠下，而这正是分栏存在所要消除的代价——没有激活订阅时也不出现，因为凡是可能渲染为空的块都属于闸门。它既不含提醒控件也不含第二个即将续费段，因为主页正上方已经显示了一条。为了不写第二份实现，统计、排序和即将续费过滤器从 `subscriptions_page.dart` 移出，进入新的、不依赖 Flutter 的 `subscription_summary.dart`（`summarizeSubscriptions`、`sortSubscriptions`、`upcomingSubscriptions`），这也顺带退役了 `finance_page.dart` 一直带着的第二份完全相同的续费过滤器私有副本；图块头像的四层回退成为共享的 `SubscriptionAvatar` 组件。**待办**——在两列或三列分区时，分区现在经新的纯函数 `columnMajorFill` 按阅读顺序填满各列，于是两列读作每日 + 日常，旁边是工作 + 评分卡片，三列各一个分区、评分卡片在工作之下；轮流发牌曾把日常放在每日旁边、工作放在其下，拆开了用户一起阅读的两个一次性清单。**亲密**——趋势图从记录历史上方移进日历窗格，那里日历下方原本空着，而图表则把第一周的历史往下推；堆叠顺序逐字节不变。窗格比例从 0.36 升到 0.42、上限从 440 升到 480，因为图表不像日历、宽度越大越受益，而 320 的下限保持不变，所以竖持的 Z Fold 5 或 7 渲染与之前完全一样；横持展开的 Fold 8 多得约 37 dp。图表的六个范围 chip 现在位于一个右对齐的 `Wrap` 中而不是跟在 `Spacer` 之后——单行时完全相同，而在下限留给它们的 288 dp 里换行而不是溢出，一条 675 x 810 的布局测试现在守着这一点。无数据格式变更，因此 WebDAV golden 转录逐字节相同。26 个新测试（共 284 个，全部通过），其中包括订阅统计和排序有史以来的第一批测试。版本统一为 `1.4.3+64` / MSIX `1.4.3.0` / 安装包 `1.4.3`。
- `v1.4.4`：体重页重排展开折叠屏上真正看到的东西。v1.4.1 给了它标准的分栏——摘要卡片待在 280–380 的固定窗格里、旁边是趋势图——而在真实的展开面板上这产生了两头不讨好的结果：卡片下方的窗格空着，而*两张*趋势图在另一半里纵向排队，于是三围图仍然在首屏之下。卡片现在不再站在窗格里，而是**展平成一条全宽横幅**：数字块（距上次多久、`displaySmall` 的体重、变化量和天数）以固定的 `weightSummaryFigureWidth` 待在左边，接着是一条 `VerticalDivider`，六个统计格——近期区间、BMI、胸围、腰围、臀围、腰臀比——占据其余部分，在桌面窗口上排成一行、在闸门处排成两行。它下方**两张趋势图各占一列**，而范围 chip 移到两列之上，那本来就是它们该在的位置：它们为体重图、三围图*以及*摘要卡片的变化量和天数挑选范围，压在其中一列上方是对它们所控制范围的误导。两张图共用一个 `Table` 行而不是待在 `Row` 的 `Expanded` 列里，这是承重的选择：两列的表头高度不同（左边是一条图例，右边是一个标题加三条图例），用 `Row` 会让一张图永久地比另一张高出那个差值，而且这个差值会在图例换行时改变。`IntrinsicHeight` 也修不了，因为 fl_chart 的 `LineChart` 根本不报告固有高度——`Table` 行是唯一能在无需测量表头的情况下对齐两个定高盒子的排布。在该排布中三围图用 220 而不是 190，因此两张图不仅顶边对齐、底边也对齐。闸门的结论没有变化，而且是刻意的：`useWeightSummaryBesideChart`（280 + 380 + 12）变成了 `useWeightChartsSideBySide`（330 + 330 + 12），两者都是 672，因此**没有任何视口改变了它所得到的布局**——竖持的 Z Fold 5 仍然堆叠，横持展开的 Fold 8 仍然分栏——并且有一条测试直接断言这个相等，而不是把它留给巧合。改变的是可分栏窗口*内部*的排布，而不是哪些窗口可分栏。`weightSummaryPaneWidth` 直接消失且没有替代品，因为等分 flex 的两列不需要窗格算术；它所钳制的两个常量也随之而去。堆叠布局在手机能产生的任何视口上都原封不动。四个新的 `ValueKey`（`weightTrendChartKey`、`weightMeasurementChartKey`、`weightSummaryFigureKey`、`weightSummaryStatsKey`）让布局测试可以比较各块的位置而不必伸手进组件内部，与 `weightSummaryKey` 和 `weightChartKey` 此前的做法一致。两个会咬人的 Flutter 陷阱记在了它们发作的源码处：直接压在 sliver 之下、带 `CrossAxisAlignment.stretch` 的 `Row` 会被交予无限高度并抛异常，而无界高度下的 `VerticalDivider` 会悄悄塌成零——横幅包在 `IntrinsicHeight` 里，一次解决两者。无数据格式变更，因此 WebDAV golden 转录逐字节相同。3 个新测试（共 287 个，全部通过），其中包括一对在渲染后页面上驱动的 n−1/n——784 x 704 堆叠对 785 x 704 并排——它钉住的是闸门在哪里翻转，而不只是规则在哪里翻转。版本统一为 `1.4.4+65` / MSIX `1.4.4.0` / 安装包 `1.4.4`。
- `v1.4.5`：三处日常改进。**Todo 表情。** 选择器的候选列表从两个任务对话框中移出——它们各自带着一份字节完全相同的私有副本——合并为一个共享的 `commonTaskEmojis`，并从 32 个扩充到 98 个，补上待办清单真正会写的日常事物：箭头、邮件（✉️ 📩 📨 📮）、包裹与快递（📦 🚚 📬）、钱款、礼物、健康、家务、宠物与植物、出行、饮食和时间。底部表单中的网格现在放在 `Flexible` 里，列表变长后会滚动，而不会在手机上溢出。**按标题建议表情。** 输入标题时，标题下方会出现一行最多八个表情标签，类似输入法的表情搜索且与界面语言无关：`suggestEmojis` 用精选的 `emojiKeywordTable`（188 个表情，每个都带英文、简体中文、繁体中文和日文关键词）为标题打分，CJK 关键词按子串匹配——这些文字没有词边界可切——拉丁关键词按整词或短语匹配，并允许一组封闭的英文词形变化（"emails"、"cooking"，但 "car" 不会匹配 "card"），仅对仍在输入中的最后一个词元做前缀匹配。刻意不按位置加分：标题通常以动词开头（"reply to email"、"取快递"），紧随其后的宾语才是更好的表情；出于同样原因，"don't forget" / 别忘了 这类填充语不收入关键词表。在用户亲自选择图标之前，图标框会**自动填充**最佳建议，建议消失时再清空，所以过时的猜测不会比产生它的文字活得更久；任何手动选择——网格、标签、自定义输入或移除——都经过同一个 `_pickEmoji`，并在本次对话框中停止自动填充，已经有表情的任务永远不会被覆盖。只有真正的文字编辑才算数：标题框会自动获得焦点，获得焦点时放置的光标同样会通知控制器，如果没有 `_lastTitleText`，仅仅打开一个任务就会自动填充它的图标，并让"取消"询问一处谁也没做过的修改。**内置银行标志（完整版构建）。** 267 个银行预设中的 219 个现在把标志放在 `assets/bank_logos/` 中一起发布（165 个 SVG、54 个规范化为 256 px 白底正方形的 PNG；共 2.7 MB），每一个都选自 Wikidata、Wikimedia Commons、Wikipedia 信息框或银行官网的候选，并肉眼核对了两遍——一遍看浏览器的绘制，一遍看 `flutter_svg` 自己的绘制。第二遍很关键：`flutter_svg` 忽略 `<style>` 样式表，导致大多数 Illustrator 导出的标志变成黑色，因此 `tool/apply_bank_logo_choices.dart` 在安装时把类规则内联到 `style` 属性中，而它仍然画不出的四个 SVG 改为发布 Wikimedia 自己的 PNG 渲染。匹配到错误机构的候选（一所大学、一家航空公司、一张电影海报）被拒绝而不是发布，其余 48 个预设——找不到可核实的标志，或官网无法访问、已被挪作他用——继续使用联网链。`tool/bank_logo_choices.json` 记录每一个决定，所以可以重新生成这组标志或移除某个标志。选择预设时，内置文件会通过 `ImageService.copyAssetImage` 复制到 `images/`——因此它与下载的标志完全一样地同步和备份——只有在没有内置标志或加载失败时，才回退到原来的八个 URL 的联网链。标志清单以 `<country>/<id>` 为键，因为预设 id 会在不同国家重复（`icbc`、`hsbc`、`vtb`……）。由于 `images/` 现在可能包含 `.svg`，每个财务头像和预览（九处）都通过 `StoredImage` / `StoredImageAvatar` 渲染，它们按扩展名选择 `flutter_svg` 或位图解码器；这也修复了一个潜在问题：联网链下载到的 Brandfetch SVG 以前永远无法显示。**商店版构建不包含任何标志。** Android 新增真正的 `full` / `store` 产品构建风味（相同的应用 id 和签名），`lib/app/build_flavor.dart` 成为 `lib/` 读取构建风味的唯一位置（`isStoreBuild` 来自 `--flavor` 或 `FLAVOR`），商店版任务在构建前运行剥离步骤 `tool/strip_bank_logos.dart`，并断言 AAB 中不含 `bank_logos`；发布的安装包文件名保持不变。Windows 没有 `--flavor`，所以桌面构建继续使用 `--dart-define=FLAVOR=full`。Android 的 `flutter run` / `build` 现在需要 `--flavor full`（或 `store`）。没有数据格式变更，所以 WebDAV 黄金记录逐字节相同。共享的布局测试辅助函数 `settleAdaptivePage` 现在让每次真实异步轮次等待几毫秒真实时间：套件变大并行运行后，挂起的文件读取有时会超出它的十二个空事件循环轮次，导致某个 Weight 或 Intimacy 布局测试找不到图表。54 个新测试（共 341 个，全部通过，连续四次完整运行）。版本统一为 `1.4.5+66` / MSIX `1.4.5.0` / 安装包 `1.4.5`。
- `v1.4.6`：银行标志显示机构的**图形，而不是文字标志**。v1.4.5 的内置标志显示在银行名称旁 24–40 px 的圆里，横版的"图形 + 名称"标志缩小后两部分都看不清，名称也只是重复旁边的文字。219 个内置标志中有 102 个现在显示图形。**59 个使用银行自己在官网发布的方形图标**——最大的 Web 应用清单图标、声明的 apple-touch-icon 或 SVG 网站图标——由新的 `tool/fetch_bank_logos.dart --icons` 抓取（每次并行六个站点，可续跑）。**43 个从完整 SVG 中裁出图形**，做法是缩小根元素的 `viewBox`（`tool/bank_logo_choices.json` 中写作 `"c0.svg#crop=x,y,w,h"`，由 `tool/apply_bank_logo_choices.dart` 中的 `cropSvg` 应用）；不修改任何路径，而 `SvgPicture` 默认裁剪到自身边界，所以被裁掉的文字永远不会显示。裁剪框来自在浏览器中测量每个形状的边界框，并在能把图形与文字分开的最粗间隔上取出属于图形的那一组（更细的间隔只会拆出单个字母）。没有独立图形的品牌——Chime、Santander、ICICI、光大银行、Lloyds、TSB 等——保留完整标志。每个改动过的 SVG 都用 `flutter_svg` 本身重新渲染，并在头像大小的圆里肉眼核对；Monzo 的官方图标 SVG 在那里渲染为空白，改为发布它的 PNG。整套标志现在是 126 个 SVG + 93 个 PNG，约 3.0 MB。除标志工具、资源和清单外没有代码或数据格式变更。341 个测试，全部通过。版本统一为 `1.4.6+67` / MSIX `1.4.6.0` / 安装包 `1.4.6`。
